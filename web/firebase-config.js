// Replace with your own Firebase project's web app config - Firebase
// console > Project settings > General > Your apps > SDK setup and
// configuration. These values identify your project; they're not secret
// (they're meant to ship in client-side JS) but they do need to be yours.
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  appId: "YOUR_APP_ID",
};
